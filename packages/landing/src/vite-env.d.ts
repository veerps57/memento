/// <reference types="vite/client" />

// Build-time constants injected by `vite.config.ts`'s `define`.
// Sourced from `git log` so the visible "Last updated" string in
// the footer (and the JSON-LD dateModified) cite a real commit
// timestamp instead of build-time randomness.
declare const __MEMENTO_LAST_MODIFIED_ISO__: string;
declare const __MEMENTO_LAST_MODIFIED_HUMAN__: string;
